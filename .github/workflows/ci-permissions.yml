name: CI

on:
  # push scoped to main so a branch with an open PR runs the pull_request_target
  # copy only, not a redundant second push run.
  push:
    branches: [main]

  pull_request_target:

jobs:
  always_succeed:
    runs-on: ubuntu-slim
    steps:
      - run: "sleep 1 && true"
